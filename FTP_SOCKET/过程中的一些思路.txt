1 是否可以考虑在FTPClient类的析构函数里调用WSACleanup函数，而其他地方不调用？

2 Disconnect函数只将socket释放，不调用WSACleanup函数

3 发送控制指令后，如果有两条返回信息，则可能一次只接收到一条,而下一条会被下一次的接受指令接收到

4 用getFirstRet()和getLastRet()解决问题3？

5 是否考虑在FTPClient中加入变量curDir来记录当前工作路径？且每次回到上一级目录或者进入下一级目录时重新调用函数getCurWorkingDir或者人为改变变量curDir的值来改变curDir，使之与当前工作路径保持一致，甚至可以考虑在界面上加一行来专门显示当前工作路径

6 是否考虑将查询目录按钮去掉，连接到服务器时就刷新目录，连接成功则登录窗口隐藏，直接刷新目录，连接失败则登录窗口不隐藏，直接弹出MessageBox,或者将现有的“连接到服务器”改为“登录信息”，然后在菜单“连接”下增加一项“登入”，用该按钮替代“查询目录”按钮,要知道隐藏窗口的函数是什么

7 注：此时我已经在每次刷新目录时重新连接


8 可以考虑将当前路径弄在一个地方一直显示






：：：：已经开启服务器上的修改功能，可以删除服务器上的文件和上传文件了
